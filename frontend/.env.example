# Frontend
# No required frontend environment variables at the moment.
# API requests use relative /api paths and are proxied by Vite to http://localhost:5000 in development.
VITE_API_BASE_URL=https://api.example.com